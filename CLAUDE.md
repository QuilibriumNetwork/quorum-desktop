# CLAUDE.md

@AGENTS.md

<!--
The line above is an IMPORT, not a note. Claude Code reads CLAUDE.md and does
not read AGENTS.md on its own, so prose saying "see AGENTS.md" left this repo's
conventions out of context entirely and relied on the model choosing to go and
open the file. The `@path` form loads it at launch, like the rest of this file.

Keep it as the whole content: AGENTS.md is the cross-tool source of truth that
Opencode, Pi and others read directly, and a second place to write rules is a
second place for them to drift.
-->
