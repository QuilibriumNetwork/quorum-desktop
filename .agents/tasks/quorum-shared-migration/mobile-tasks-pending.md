---
type: tracker
title: Mobile tasks pending (unified — all mobile-bound work)
status: ongoing
created: 2026-05-28
audience: future sessions wanting to see what's queued on the mobile side
scope: "ALL mobile tasks dropped from desktop sessions — shared-migration adoptions, desktop→mobile feature ports, and convergence work. Single source of truth."
---

# Mobile tasks pending — the unified tracker

> **This is the single tracker for every mobile-bound task**, regardless of where it originated: shared-package adoptions (this folder's work), desktop→mobile feature ports, and convergence work (see [../port-to-mobile/candidates.md](../port-to-mobile/candidates.md)). The `Category` column records the origin so provenance is never lost. It lives in the `quorum-shared-migration/` folder for historical reasons (many workflow docs point at this exact path), but its role is cross-cutting — do **not** create a second mobile-task tracker elsewhere; add rows here.
>
> Mobile's `.agents/` is gitignored, so mobile task files have no GitHub visibility. This table is the desktop-side bookkeeping: a list of mobile tasks dropped during our sessions, so we don't lose track of work that's been handed off.
>
> **Maintenance**: when a mobile task file moves to `.done/` (or is closed), update the row here. New rows are inserted in the row's correct urgency tier (NOT appended at the bottom — the table is sorted by urgency, then by drop date within a tier). Set the `Category` (`shared-migration` / `feature-port` / `convergence`) to match the task's origin; for a candidate graduating from [../port-to-mobile/candidates.md](../port-to-mobile/candidates.md), use the same value as its `Type` there. When you flip a row's urgency, re-sort and add a footer note explaining why.
>
> **Shared version note for executors**: task files written before today may pin older shared versions (e.g. `2.1.0-18`, `2.1.0-19`). Those are "available since" floors, not exact pins. When picking up any task, bump mobile's `@quilibrium/quorum-shared` to the **latest published version** (currently `2.1.0-28` as of 2026-06-10 — check npm for newer). Row 2 (profile-validation convergence) specifically **requires** `2.1.0-28`, which is where the Farcaster byte limits (`MAX_DISPLAY_NAME_BYTES`, `MAX_BIO_BYTES`) and the `.q` dot rule landed; `MAX_BIO_LENGTH` was removed in that release.

## Urgency tier legend

Tier reflects user-visible impact and irreversibility. Sequencing recommendation, not a deadline.

| Tier | Meaning |
|---|---|
| 🔴 Tier 1 | Real user-visible bug. Production users are affected today. Sequence first. |
| 🟠 Tier 2 | Silent correctness/security hole or cross-platform inconsistency with a real-world footprint. Sequence next. |
| 🟡 Tier 3 | Cleanup bundled with minor correctness wins (XSS defence-in-depth, timer leaks, masked bugs). Worth doing but not urgent. |
| 🟢 Tier 4 | Pure mechanical refactor, byte-for-byte equivalent, no runtime test required. Batch onto any unrelated PR. |
| 🚧 Blocked | Waiting on a lead-dev product call before scoping. |

## Category legend

Where the task originated. Provenance only — does not affect sequencing (urgency does).

| Category | Meaning |
|---|---|
| `shared-migration` | Mobile should adopt a `@quilibrium/quorum-shared` export (this folder's core work). The original and most common category. |
| `feature-port` | A desktop feature mobile lacks entirely. Graduated from [../port-to-mobile/candidates.md](../port-to-mobile/candidates.md) (`Type: feature-port`). |
| `convergence` | Mobile has the capability but desktop's implementation is better; mobile converges. Graduated from [../port-to-mobile/candidates.md](../port-to-mobile/candidates.md) (`Type: convergence`). |

## Currently queued

Sorted by urgency tier, then by drop date within a tier.

| Urgency | Category | Drop date | Task file | What it covers | Triggered by (desktop PR) | Runtime test? | Status |
|---|---|---|---|---|---|---|---|
| 🔴 1 | shared-migration | 2026-05-29 | [`2026-05-29-channel-reorder-mutations-should-broadcast.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/2026-05-29-channel-reorder-mutations-should-broadcast.md) | Wire `broadcastSpaceUpdate` into `useMoveChannel`, `useReorderGroups`, `useReorderChannels` in mobile's `hooks/chat/useChannelManagement.ts`. Currently local-only writes → mobile-initiated reorders never sync to other devices/desktop. Desktop reorders sync fine. Cross-platform sync asymmetry. Also a **prerequisite for the C4 pure-helpers shared promotion** (`moveChannelInSpace`/`reorderGroupsInSpace`/`reorderChannelsInGroup` — see [roadmap.md "Paused tracks"](roadmap.md#paused-tracks-not-in-the-linear-sequence)). Lives outside `quorum-shared-migration/` on mobile because it's a correctness fix, not a shared-adoption task — but the tracker's scope is "anything dropped on mobile during our work", so it belongs here. Tracked publicly as [mobile#66](https://github.com/QuilibriumNetwork/quorum-mobile/issues/66). **Why Tier 1**: end-user-visible cross-device sync bug. Mobile-initiated channel reorders never reach desktop or other devices. | [quorum-desktop `2026-01-07-channel-ordering-feature`](https://github.com/QuilibriumNetwork/quorum-desktop) (desktop ships drag-and-drop reorder, broadcasts correctly — surfaces the mobile gap by asymmetry) | ✅ required | 📋 open |
| 🟠 2 | shared-migration | 2026-06-08 | [`2026-06-10-mobile-converge-profile-validation-to-shared.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-06-10-mobile-converge-profile-validation-to-shared.md) | **Converge mobile's bio AND display-name inputs onto shared's byte validators, then hard-block at the Farcaster publish boundary.** Three bio editors (`profile-setup` 160, `UnifiedProfileEditModal` 256, `SpaceSettingsModal` 280) + two display-name inputs (`profile-setup`/`ProfileModal` 50, `UnifiedProfileEditModal` 60) all currently use hardcoded `maxLength` and run `.trim()`-only — **no XSS, no impersonation/homoglyph, no reserved-mention, no `.q` check, no byte cap**. Route them through shared `validateUserBio` + `validateDisplayName` (`@quilibrium/quorum-shared@2.1.0-28`), which now enforce **Farcaster `USER_DATA` byte limits**: display name ≤ **32 bytes** (`MAX_DISPLAY_NAME_BYTES`), bio ≤ **256 bytes** (`MAX_BIO_BYTES`). Then add a hard guard in `services/farcaster/updateProfile.ts` / `UnifiedProfileEditModal.saveFarcaster()` so a value over the byte limit is blocked **before** the `PATCH /v2/me` (Farcaster's relay rejects oversize values — without the guard the user just gets an opaque `HTTP 400`). **Net cap changes (user-visible, runtime test required):** bio onboarding 160→256B (gains room), bio per-space 280→256B (shrinks), display name 50/60→32B (shrinks). Also adopts the new `.q`/dot rule (`'qns-suffix'`) protecting the QNS verified-name trust marker. **Why Tier 2 (security + correctness):** (a) no XSS/impersonation/`.q` check on mobile today — once desktop ships QNS-usernames (renders verified `name.q` in accent color), mobile's missing dot rule lets anyone spoof a verified handle (`admin.q`) cross-platform; (b) without the byte caps + publish-boundary guard, oversize names/bios reach Farcaster and get silently rejected. | [quorum-shared#37](https://github.com/QuilibriumNetwork/quorum-shared/pull/37) (Farcaster byte limits, merged → `2.1.0-28`) + [quorum-desktop `feat/farcaster-aligned-validation-limits`](https://github.com/QuilibriumNetwork/quorum-desktop) (desktop consumes the same validators) | ✅ required | 📋 open |
| 🟠 2.5 | shared-migration | 2026-06-11 | [`2026-06-11-mobile-honor-displayname-clear.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-06-11-mobile-honor-displayname-clear.md) | **Honor an empty `displayName` as a per-space-name clear in mobile's `update-profile` receiver.** Desktop now lets a user clear their per-space name (empty field = use global/QNS name), broadcast as `displayName: ''`. Mobile's receiver (`context/WebSocketContext.tsx`, BOTH handlers — ~line 1893 and ~line 3014) uses a truthy guard `...(profileContent.displayName ? { display_name } : {})`, so it **ignores** the empty clear → a desktop user who clears their per-space name still shows their OLD name to mobile users. Change both guards to `!== undefined` (mirroring the `bio` line right beside them). Requires bumping shared to **2.1.0-29** (where `UpdateProfileMessage.displayName` became optional). **Why Tier 2 (cross-platform inconsistency):** stale-label-never-wrong-target; desktop→desktop clearing works, desktop→mobile shows a stale per-space name until mobile adopts the same receiver logic. | [quorum-shared `feat/optional-update-profile-displayname`](https://github.com/QuilibriumNetwork/quorum-shared) (→ 2.1.0-29, displayName optional) + [quorum-desktop `feat/qns-username-overrides-display-name`](https://github.com/QuilibriumNetwork/quorum-desktop) (sender omits/clears displayName; desktop receiver already uses `!== undefined`) | ✅ required | 📋 open |
| 🟡 3 | shared-migration | 2026-05-28 | [`2026-05-28-adopt-shared-validators.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-05-28-adopt-shared-validators.md) | Drop mobile's local `validateSpaceName` + inline length constants in `SpaceModal.tsx` and `SpaceSettingsModal.tsx`; consume `@quilibrium/quorum-shared@2.1.0-19` validators via a thin English-string translator. Adds XSS check on space name (defense-in-depth). **Why Tier 3**: cleanup bundled with a real security-defence-in-depth improvement (XSS check on space name). No acute bug, but the XSS check is a real-world value-add. | [quorum-desktop#162](https://github.com/QuilibriumNetwork/quorum-desktop/pull/162) (validation hooks migration) | ✅ required | 📋 open |
| 🟡 4 | shared-migration | 2026-05-29 | [`2026-05-29-mobile-adopt-shared-permission-helpers.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-05-29-mobile-adopt-shared-permission-helpers.md) | Refactor mobile's `useHasPermission`/`useUserPermissions`/`useUserRoles` (in `hooks/chat/useRoleManagement.ts:56-115`) to delegate to shared's existing `hasPermission`/`getUserPermissions`/`getUserRoles` pure functions. Removes ~60 LOC of duplicated logic. **2026-06-09 call-site audit:** the "owner permissions silently dropped" bug is real at the hook level but fully masked at the call site — `useHasPermission` has exactly ONE caller (`[channelId].tsx` lines 51-59) that already OR's `isSpaceOwner` after the call (`hasPinPermission = hasRolePin \|\| isSpaceOwner`); the other two hooks have zero callers. No user-visible regression today. Value is ~60 LOC cleanup + defence-in-depth against future callers. **Why Tier 3 (downgraded from Tier 2 on 2026-06-09)**: bug is real at the hook level but masked at the only call site. Defence-in-depth against future callers, not a production bug. | Phase 2 verification 2026-05-29 (no triggering PR — finding from desktop's `useRoleManagement` audit) | ✅ required | 📋 open (downgraded urgency 2026-06-09 — see mobile task file) |
| 🟡 5 | shared-migration | 2026-05-29 | [`2026-05-29-mobile-adopt-useTwoStepConfirm-in-useUserKicking.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-05-29-mobile-adopt-useTwoStepConfirm-in-useUserKicking.md) | Replace inlined two-step confirmation state machine in mobile's `hooks/chat/useUserKicking.ts` with shared's `useTwoStepConfirm` (already at `2.1.0-18`). Desktop already adopted in [#161](https://github.com/QuilibriumNetwork/quorum-desktop/pull/161). Mobile is the last inline holdout. Public hook surface unchanged. **Why Tier 3**: shared hook stores timeout in `useRef` (not `useState`) + has an unmount-cleanup effect, fixing a tiny memory leak when the user unmounts mid-confirm. Small surface, real-but-minor correctness win. | [quorum-shared#19](https://github.com/QuilibriumNetwork/quorum-shared/pull/19) + [quorum-desktop#161](https://github.com/QuilibriumNetwork/quorum-desktop/pull/161) | ✅ required | 📋 open |
| 🟢 6 | shared-migration | 2026-05-30 | [`2026-05-30-mobile-adopt-shared-role-mutation-helpers.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-05-30-mobile-adopt-shared-role-mutation-helpers.md) | Adopt shared `toggleRolePermission` (from `@quilibrium/quorum-shared@2.1.0-21`) in `useToggleRolePermission.mutationFn` (`hooks/chat/useRoleManagement.ts`). Pure mechanical refactor, byte-for-byte equivalent. `useUpdateRole.mutationFn` stays inline (multi-field update, out of scope). **Why Tier 4**: pure mechanical refactor, byte-for-byte equivalent. No runtime test required. Tack onto any unrelated PR touching `useRoleManagement.ts`. | [quorum-shared#21](https://github.com/QuilibriumNetwork/quorum-shared/pull/21) + [quorum-desktop#163](https://github.com/QuilibriumNetwork/quorum-desktop/pull/163) | ❌ not required | 📋 open |
| 🟢 7 | shared-migration | 2026-05-30 | [`2026-05-30-mobile-dedup-deriveAddress.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-05-30-mobile-dedup-deriveAddress.md) | Dedupe `deriveAddress` — 3 local copies (`spaceService`, `useChannelManagement`, `useSpaceActions`) → 1 import from `services/onboarding/keyService.ts`. Mobile-internal cleanup, no shared change, ~15 LOC removed. Surfaced during bonus C1 sweep. **Why Tier 4**: smallest possible task. Pure cleanup, ~15 LOC across 3 files. Zero risk, zero behaviour change. Tack onto any unrelated PR touching those three files. **2026-06-10 update**: the desktop QNS-usernames work promotes `deriveAddress` INTO `@quilibrium/quorum-shared` (`src/qns/deriveAddress.ts`, ported from mobile's keyService impl). When that lands, the dedup target should become "all copies (incl. keyService's) import from shared," collapsing this from a mobile-internal dedup into a shared-adoption task. Revisit after the QNS shared PR publishes. | None — finding from 2026-05-30 mobile sweep | ❌ not required | 📋 open |
| 🚧 | shared-migration | 2026-05-29 | [`2026-05-29-mobile-rewire-invite-helpers-to-shared.md`](file:///D:/GitHub/Quilibrium/quorum-mobile/.agents/tasks/quorum-shared-migration/2026-05-29-mobile-rewire-invite-helpers-to-shared.md) | Mobile has THREE local reimplementations of invite helpers shared already exports: `getInviteUrlBase` (in `services/space/inviteService.ts:68-72`), `VALID_INVITE_PREFIXES` (in `inviteService.ts:26-36` AND `hooks/chat/useSpaceActions.ts:26-36`), `parseInviteLink` (in both files). Mobile's `getInviteUrlBase` hardcodes the prod domain — staging/localhost builds generate wrong invite links. Rewire all three to shared's exports (`getInviteUrlBase`, `getValidInvitePrefixes`, `parseInviteParams`). ~80 LOC removed + correctness fix. | Phase 2 verification 2026-05-29 (no triggering PR — finding from desktop's manifest/invite audit) | ✅ required | 🚧 **blocked — awaiting lead-dev call (2026-06-09)**. The only behaviour-changing piece is env-aware domain detection for staging/localhost mobile builds. If those build targets aren't supported on mobile (reasonable product call for an app-store-shipped app), the "correctness fix" framing doesn't apply and what's left is ~80 LOC of pure cleanup + a wider `parseInviteParams` return shape requiring null-guards at call sites. That's a "nice cleanup, not worth the runtime-test cycle" profile. Confirm with mobile lead whether staging/localhost mobile builds are in scope; if not, close as won't-do. |

## Completed

(empty)

---

*Last updated: 2026-06-12 — **promoted this file to the single unified mobile-task tracker.** Added a `Category` column (`shared-migration` / `feature-port` / `convergence`) + a Category legend, and reframed the title/intro: it now tracks ALL mobile-bound work, not just shared-migration adoptions. All eight existing rows are `shared-migration`. Rationale: the new [../port-to-mobile/](../port-to-mobile/) folder catalogues desktop→mobile candidates (feature-ports + convergence); rather than spawn a second tracker there, those candidates graduate into rows HERE, keeping one source of truth. The file stays at this path (not moved) because ~10 workflow docs + frozen `.done/` files point at this exact location. See [../port-to-mobile/README.md](../port-to-mobile/README.md).*

*Previously: 2026-06-11 — added row **2.5** (🟠 Tier 2): mobile honor-displayName-clear. Surfaced shipping the desktop QNS-usernames feature, which lets users clear their per-space name (empty = use global/QNS name). The clear broadcasts `displayName: ''`; mobile's `update-profile` receiver (`WebSocketContext.tsx`, both handlers) uses a truthy guard so it ignores the empty clear → desktop users who clear show a stale name to mobile. Requires shared `2.1.0-29` (`UpdateProfileMessage.displayName` made optional on branch `feat/optional-update-profile-displayname`). Placed between rows 2 and 3 (sorted by tier, then drop date) — labelled 2.5 to avoid renumbering existing rows. Mobile task file created: `2026-06-11-mobile-honor-displayname-clear.md`.*

*Previously: 2026-06-10 (later) — **merged rows 2 and 2b into a single corrected row 2** after shared [#37](https://github.com/QuilibriumNetwork/quorum-shared/pull/37) merged (→ `2.1.0-28`). That PR made the bio and display-name validators **byte-based to match Farcaster's `USER_DATA` limits** (display ≤ 32 bytes, bio ≤ 256 bytes) and **removed `MAX_BIO_LENGTH`**. The old rows were stale: row 2 targeted the now-deleted `MAX_BIO_LENGTH=160` and described the cap change as a reduction (it's actually a rise to 256B for the global bio; only the per-space 280 editor shrinks); row 2b's "blocked on the dot-rule shared PR" is unblocked (the dot rule shipped in #37 alongside the byte caps). The merged row covers the same editors for both fields, the corrected byte facts, and the net-new **Farcaster publish-boundary hard-block** in `saveFarcaster()` (oversize values must be rejected before `PATCH /v2/me`, not silently 400'd by the relay). Updated the shared-version note to `2.1.0-28`. **Mobile task files** `2026-06-08-mobile-converge-bio-length-to-shared.md` and `2026-06-10-mobile-adopt-shared-display-name-validation.md` (in mobile's gitignored `.agents/`) should be consolidated into `2026-06-10-mobile-converge-profile-validation-to-shared.md` to match — not done from desktop (no visibility into mobile's tree).*

*Previously: 2026-06-10 — added row **2b** (🟠 Tier 2, security): mobile adopt shared `validateDisplayName`. Surfaced during the desktop QNS-usernames design — mobile runs `.trim()`-only on display names (no XSS / impersonation / reserved / `.q` check), so the verified `name.q` trust marker the desktop QNS feature introduces would be spoofable from mobile. Blocked on the shared PR that adds the `.q`/dot rule. Also updated row 7 (`deriveAddress` dedup): the desktop QNS work promotes `deriveAddress` into `quorum-shared`, so that task should graduate from a mobile-internal dedup to a shared-adoption task once the QNS shared PR publishes. Mobile task file created: `2026-06-10-mobile-adopt-shared-display-name-validation.md`.*

*Previously: 2026-06-09 — added Urgency column and re-sorted the "Currently queued" table by urgency tier (🔴/🟠/🟡/🟢/🚧). Added an "Urgency tier legend" section above the table explaining tier semantics. Each row now carries a one-line "**Why Tier N**" justification inside the "What it covers" cell. Updated the Maintenance note (new rows go in the right tier, not appended at the bottom). The chronological ordering is preserved as a secondary sort within each tier. Tier assignments come from the 2026-06-09 ranking analysis: Tier 1 = channel-reorder broadcast (user-visible cross-device sync bug). Tier 2 = bio-length convergence (missing XSS + cap mismatch). Tier 3 = validators adoption, permission helpers (downgraded), useTwoStepConfirm. Tier 4 = role-mutation helpers, deriveAddress dedup. Blocked = invite-helpers rewire.*

*Previously: 2026-06-09 — call-site audit of `2026-05-29-mobile-adopt-shared-permission-helpers.md`. The "owner permissions silently dropped" bug originally framed as a correctness fix is masked at the only call site (`useHasPermission` has one caller that already OR's `isSpaceOwner`; the other two hooks have zero callers). Bug is latent risk, not user-visible regression. Task value is now ~60 LOC cleanup + defence-in-depth, runtime-test cost unchanged — net urgency drops from medium-high to low-medium. Row note + mobile task file updated with the audit findings and a re-prioritisation block.*

*Previously: 2026-06-09 — `2026-05-29-mobile-rewire-invite-helpers-to-shared.md` flipped from 📋 open to 🚧 blocked-on-lead-dev-call. The original framing leaned on env-aware domain detection (staging/localhost builds generate prod URLs) as the "correctness fix" that justified the runtime-test cost; if mobile doesn't support staging/localhost build targets (reasonable product call), what's left is ~80 LOC of pure cleanup plus call-site adjustments for shared's wider `parseInviteParams` return shape — not worth the test cycle on its own. Awaiting lead-dev confirmation; close as won't-do if staging/localhost builds aren't in scope. Mobile-side task file also updated with the same status.*

*Previously: 2026-06-09 — added `2026-05-29-channel-reorder-mutations-should-broadcast.md` after a fresh scan of mobile's `.agents/tasks/`. The task file has existed since 2026-05-29 but was never registered here, likely because it lives one folder up (in `.agents/tasks/` directly, not under `quorum-shared-migration/`). It is referenced from `roadmap.md` "Paused tracks" as a prerequisite to the C4 pure-helpers shared promotion. All other task files on disk are already tracked.*

*Created 2026-05-28. Workflow rationale in [cross-repo-workflow.md](cross-repo-workflow.md) section "Proactive mobile task drop".*
