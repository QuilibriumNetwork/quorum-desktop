/// <reference types="vite/client" />

declare const __INCLUDE_PLAYGROUND_WEB__: boolean;

/**
 * App version inlined by Vite from package.json. Only defined in Vite builds —
 * read it through `APP_VERSION` in src/config/appVersion.ts, which guards the
 * environments (Metro, Vitest) where it does not exist.
 */
declare const __APP_VERSION__: string;
