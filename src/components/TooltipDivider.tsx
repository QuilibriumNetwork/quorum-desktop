import * as React from 'react';

import './TooltipDivider.scss';

const TooltipDivider: React.FunctionComponent<{}> = (props) => {
  return <div className="quorum-tooltip-divider" />;
};

export default TooltipDivider;
